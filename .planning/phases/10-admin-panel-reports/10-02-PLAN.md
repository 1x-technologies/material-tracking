---
phase: 10-admin-panel-reports
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - apps/web/src/pages/AdminPage.tsx
  - apps/web/src/pages/PendingApprovalPage.tsx
  - apps/web/src/components/admin/AdminTabs.tsx
  - apps/web/src/components/admin/SidePanel.tsx
  - apps/web/src/components/admin/StatusPill.tsx
  - apps/web/src/components/admin/UserTable.tsx
  - apps/web/src/components/admin/UserDetailPanel.tsx
  - apps/web/src/components/layout/Sidebar.tsx
  - apps/web/src/context/AuthContext.tsx
  - apps/web/src/App.tsx
autonomous: true
requirements:
  - ADMN-01

must_haves:
  truths:
    - "Admin sees /admin page with Users tab active by default"
    - "Admin can view all users in a table with name, email, role, location, status columns"
    - "Admin can click a user row to open a side panel with edit controls"
    - "Admin can change a user's role via the side panel and save"
    - "Admin can deactivate/reactivate a user with double-confirm pattern"
    - "Admin can select multiple users via checkboxes and bulk-assign a role"
    - "Pending users (role: null) see the PendingApprovalPage instead of the app"
    - "Admin nav item appears in sidebar only for admin role"
  artifacts:
    - path: "apps/web/src/pages/AdminPage.tsx"
      provides: "Admin page shell with tab navigation"
      contains: "AdminTabs"
    - path: "apps/web/src/pages/PendingApprovalPage.tsx"
      provides: "Pending approval page for users with null role"
      contains: "Account Pending Approval"
    - path: "apps/web/src/components/admin/SidePanel.tsx"
      provides: "Reusable slide-in panel wrapper"
      contains: "translate-x"
    - path: "apps/web/src/components/admin/UserTable.tsx"
      provides: "User list table with checkbox selection"
      contains: "Select all users"
    - path: "apps/web/src/components/admin/UserDetailPanel.tsx"
      provides: "User edit side panel"
      contains: "Save Changes"
  key_links:
    - from: "apps/web/src/App.tsx"
      to: "apps/web/src/pages/AdminPage.tsx"
      via: "Route path='admin' with RequireRole admin"
      pattern: "path.*admin"
    - from: "apps/web/src/App.tsx"
      to: "apps/web/src/pages/PendingApprovalPage.tsx"
      via: "Conditional render when appUser.role is null"
      pattern: "PendingApprovalPage"
    - from: "apps/web/src/components/admin/UserTable.tsx"
      to: "trpc.admin.listUsers"
      via: "tRPC query hook"
      pattern: "trpc\\.admin\\.listUsers"
    - from: "apps/web/src/components/admin/UserDetailPanel.tsx"
      to: "trpc.admin.updateUser"
      via: "tRPC mutation hook"
      pattern: "trpc\\.admin\\.updateUser"
---

<objective>
Build the admin page shell, pending approval page, reusable admin components (SidePanel, StatusPill, AdminTabs), and the complete User Management tab with table, detail panel, checkbox selection, and bulk role assignment.

Purpose: Delivers the primary admin interface for user management (ADMN-01) and the pending user experience (D-02, D-14). The SidePanel and StatusPill components are reusable for Location Management in Plan 03.
Output: Functional /admin route with Users tab, reusable SidePanel component, PendingApprovalPage, and updated sidebar/routing.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-admin-panel-reports/10-CONTEXT.md
@.planning/phases/10-admin-panel-reports/10-UI-SPEC.md
@.planning/phases/10-admin-panel-reports/10-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan consumes -->

From apps/api/src/routers/admin.ts (created in Plan 01):
```typescript
// tRPC procedures available via trpc.admin.*
listUsers: adminProcedure.query(...)     // Returns Array<{ uid, email, displayName, role, locationId, active, createdAt, lastLoginAt }>
updateUser: adminProcedure.input(updateUserSchema).mutation(...)  // Returns { success: true }
bulkAssignRole: adminProcedure.input(bulkAssignRoleSchema).mutation(...)  // Returns { updated: N, failed: N }
listAllLocations: adminProcedure.query(...)  // For location dropdown in user detail
```

From packages/shared/src/schemas/admin.ts (created in Plan 01):
```typescript
export const updateUserSchema = z.object({
  uid: z.string(),
  patch: z.object({
    role: z.enum(["admin","driver","staff"]).nullable().optional(),
    locationId: z.string().optional(),
    active: z.boolean().optional(),
  })
});
export const bulkAssignRoleSchema = z.object({
  uids: z.array(z.string()).min(1),
  role: z.enum(["admin","driver","staff"]),
});
```

From apps/web/src/context/AuthContext.tsx:
```typescript
export interface AppUser {
  role: UserRole;    // Will need to change to UserRole | null
  displayName: string;
  email: string;
  locationId: string;
}
```

From apps/web/src/components/layout/Sidebar.tsx:
```typescript
const navItems: NavItem[] = [
  { label: "Dashboard", path: "/dashboard", icon: "...", roles: allRoles },
  { label: "History", path: "/history", icon: "...", roles: ["staff", "admin"] },
  { label: "New Shipment", path: "/shipments/new", icon: "...", roles: ["staff", "admin"] },
  { label: "Scan", path: "/scan", icon: "...", roles: ["driver", "admin"] },
];
```

From apps/web/src/components/dashboard/FilterTabs.tsx (pattern reference):
```typescript
interface Tab { id: string; label: string; count: number; }
// Tab bar with role="tablist", aria-selected, border-b active indicator
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable admin components (SidePanel, StatusPill, AdminTabs) and PendingApprovalPage</name>
  <files>
    apps/web/src/components/admin/SidePanel.tsx,
    apps/web/src/components/admin/StatusPill.tsx,
    apps/web/src/components/admin/AdminTabs.tsx,
    apps/web/src/pages/PendingApprovalPage.tsx,
    apps/web/src/context/AuthContext.tsx,
    apps/web/src/App.tsx,
    apps/web/src/components/layout/Sidebar.tsx
  </files>
  <read_first>
    apps/web/src/context/AuthContext.tsx,
    apps/web/src/App.tsx,
    apps/web/src/components/layout/Sidebar.tsx,
    apps/web/src/components/dashboard/FilterTabs.tsx,
    apps/web/src/components/auth/RequireRole.tsx,
    apps/web/src/components/layout/AppLayout.tsx,
    apps/web/src/pages/AccessDeniedPage.tsx
  </read_first>
  <action>
    **1. SidePanel.tsx** -- Reusable slide-in panel wrapper (per UI-SPEC "Table + Side Panel Pattern"):
    ```
    Props: { open: boolean; onClose: () => void; title: string; subtitle?: string; children: ReactNode }
    ```
    - Fixed position, right side, `w-[400px]` on lg+, full-screen overlay on < lg (1024px)
    - `transition-transform duration-200 ease-out` for slide animation
    - When open: `translate-x-0`, when closed: `translate-x-full`
    - `bg-white border-l border-neutral-200 shadow-lg`
    - Close button: top-right "X" character, `size-11` touch target, `aria-label="Close panel"`
    - `role="dialog"` with `aria-label={title}`
    - Add `useEffect` for Escape key: `document.addEventListener("keydown", handler)` that calls `onClose` when key === "Escape"
    - On < lg: add semi-transparent backdrop `bg-black/20` behind panel, clicking backdrop calls onClose
    - Render header with title at `text-lg font-semibold` and subtitle at `text-sm text-neutral-500`

    **2. StatusPill.tsx** -- Status badge for active/inactive/pending:
    ```
    Props: { status: "active" | "inactive" | "pending" }
    ```
    - Active: `bg-green-100 text-green-700`
    - Inactive: `bg-neutral-100 text-neutral-500`
    - Pending: `bg-amber-100 text-amber-700`
    - Shared: `inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium`
    - Capitalize the status text

    **3. AdminTabs.tsx** -- Hash-based tab navigation (per UI-SPEC tab spec, adapted from FilterTabs pattern):
    ```
    Props: { tabs: Array<{ id: string; label: string; count?: number }>; activeTab: string; onTabChange: (id: string) => void }
    ```
    - Uses `role="tablist"`, each tab `role="tab"` with `aria-selected`
    - Active tab: `border-b-2 border-brand-600 text-brand-700 font-semibold`
    - Inactive: `text-neutral-500 hover:text-neutral-700`
    - Tab count badge: `bg-neutral-100 text-neutral-600 rounded-full px-2 py-0.5 text-xs font-medium ml-1.5` (shown when count is defined and > 0)

    **4. PendingApprovalPage.tsx** -- Full page for users with null role (per D-14, UI-SPEC section 6):
    - Centered vertically and horizontally: `min-h-screen flex items-center justify-center`
    - Inner container: `max-w-md p-6 text-center`
    - Clock emoji: `text-5xl text-neutral-400 mb-4` (use unicode character)
    - Heading: "Account Pending Approval" at `text-lg font-semibold text-neutral-900`
    - Body: "Your account is pending approval. An administrator will assign your role shortly." at `text-sm text-neutral-600 max-w-sm text-center mt-2`
    - Sign out button: `mt-6`, `rounded-md border border-neutral-300 bg-white px-4 py-2 text-sm font-semibold text-neutral-700 hover:bg-neutral-50`
    - Calls `signOutUser` from `useAuthContext()`

    **5. AuthContext.tsx** -- Update AppUser type for nullable role (per D-01/D-02):
    - Change `role: UserRole` to `role: UserRole | null` on the AppUser interface
    - In `loadProfile`, when reading Firestore doc data, set `role: (data.role as UserRole) ?? null` instead of `role: data.role as UserRole`

    **6. App.tsx** -- Add admin route and pending user detection:
    - Import `AdminPage` and `PendingApprovalPage`
    - In `AuthGate`, after the loading checks and after confirming user exists, add a check: if `appUser` exists and `appUser.role === null`, render `<PendingApprovalPage />` instead of children. This goes BEFORE the `<>{children}</>` return.
    - Add admin route inside `AuthenticatedRoutes` within the `<Route element={<AppLayout />}>` block:
      ```tsx
      <Route
        path="admin"
        element={
          <RequireRole allowedRoles={["admin"]}>
            <AdminPage />
          </RequireRole>
        }
      />
      ```

    **7. Sidebar.tsx** -- Add Admin nav item:
    - Add to navItems array: `{ label: "Admin", path: "/admin", icon: "\u2699\uFE0F", roles: ["admin"] }`
    - Place it as the last item in the array
    - Note: the Sidebar filters by `appUser.role` which is now nullable. The filter `item.roles.includes(appUser.role)` will return false for null, which is correct -- pending users don't see any nav items. BUT the Sidebar only renders inside AppLayout which is inside AuthenticatedRoutes, and pending users are intercepted by AuthGate before reaching AppLayout. So no Sidebar change needed for pending users. Just add the Admin nav item.
    - IMPORTANT: Since appUser.role can now be null and `includes()` expects UserRole, cast or handle: change the filter to `appUser && appUser.role && item.roles.includes(appUser.role)`.
  </action>
  <verify>
    <automated>cd /Users/quang.nguyen/development/material-tracking && pnpm --filter web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/admin/SidePanel.tsx exists and contains `role="dialog"`, `translate-x`, `aria-label="Close panel"`, and Escape key handler
    - apps/web/src/components/admin/StatusPill.tsx exports StatusPill with bg-green-100, bg-neutral-100, bg-amber-100 variants
    - apps/web/src/components/admin/AdminTabs.tsx exports AdminTabs with `role="tablist"` and `aria-selected`
    - apps/web/src/pages/PendingApprovalPage.tsx contains "Account Pending Approval" and "Sign Out" button
    - apps/web/src/context/AuthContext.tsx AppUser interface has `role: UserRole | null`
    - apps/web/src/App.tsx contains import and Route for AdminPage at path "admin" with RequireRole ["admin"]
    - apps/web/src/App.tsx AuthGate renders PendingApprovalPage when `appUser.role === null`
    - apps/web/src/components/layout/Sidebar.tsx contains Admin nav item with path "/admin" and roles ["admin"]
    - `pnpm --filter web exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Reusable SidePanel, StatusPill, AdminTabs components created; PendingApprovalPage shows for null-role users; /admin route registered with RequireRole admin; Admin entry in sidebar</done>
</task>

<task type="auto">
  <name>Task 2: Build AdminPage shell and User Management tab (table, detail panel, bulk operations)</name>
  <files>
    apps/web/src/pages/AdminPage.tsx,
    apps/web/src/components/admin/UserTable.tsx,
    apps/web/src/components/admin/UserDetailPanel.tsx
  </files>
  <read_first>
    apps/web/src/components/admin/SidePanel.tsx,
    apps/web/src/components/admin/StatusPill.tsx,
    apps/web/src/components/admin/AdminTabs.tsx,
    apps/web/src/components/dashboard/FilterTabs.tsx,
    apps/web/src/pages/HistoryPage.tsx,
    apps/web/src/components/shipment/CancelShipmentButton.tsx,
    apps/web/src/trpc.tsx
  </read_first>
  <action>
    **1. AdminPage.tsx** -- Page shell with tab navigation (per D-13, UI-SPEC section 1):
    - Page heading: "Admin" at `text-2xl font-semibold text-neutral-900`
    - Subtitle: "Manage users, locations, settings, and reports" at `text-sm text-neutral-500 mt-1`
    - Tab state from URL hash: `const [activeTab, setActiveTab] = useState(() => window.location.hash.slice(1) || "users")`
    - On tab change: `setActiveTab(id)` and `window.location.hash = id`
    - Listen for `hashchange` event to sync tab state with browser back/forward
    - Tabs: `[{ id: "users", label: "Users", count: userCount }, { id: "locations", label: "Locations", count: locationCount }, { id: "settings", label: "Settings" }, { id: "reports", label: "Reports" }]`
    - Counts come from tRPC queries: `trpc.admin.listUsers` and `trpc.admin.listAllLocations` (use `.data?.length`)
    - Render `<AdminTabs>` component
    - Conditional content: when activeTab === "users" render `<UserTable />`; "locations" render placeholder `<div>Locations tab coming in Plan 03</div>`; "settings" render placeholder; "reports" render placeholder
    - At bottom (outside tab content): render placeholder `<div><!-- AuditLog will be added in Plan 03 --></div>`

    **2. UserTable.tsx** -- Full user management table (per D-03, D-05, D-16, UI-SPEC section 2):
    - Data: `const { data: users, isLoading } = trpc.admin.listUsers.useQuery()`
    - Also load locations for display: `const { data: locations } = trpc.admin.listAllLocations.useQuery()`
    - State: `selectedIds: Set<string>`, `selectedUserId: string | null` (for detail panel), `searchQuery: string`, `bulkDropdownOpen: boolean`
    - Search: filter users client-side by `searchQuery` matching name or email (case-insensitive)
    - Toolbar: left side -- "Assign Role" button (per D-05/D-16). Text: `Assign Role${selectedIds.size > 0 ? ` (${selectedIds.size})` : ""}`. Disabled when selectedIds.size === 0 (use `bg-neutral-100 text-neutral-400 cursor-not-allowed` vs `bg-brand-600 text-white`). On click, toggle `bulkDropdownOpen`.
    - Bulk dropdown: render below button when `bulkDropdownOpen === true`. Three options: Admin, Driver, Staff. Each is a button. On click, call `bulkAssignMutation.mutate({ uids: [...selectedIds], role })`, close dropdown, clear selectedIds, show toast `Role updated for ${selectedIds.size} users`.
    - Right side: search input `w-64` with placeholder "Search users...", `rounded-md border border-neutral-300 px-3 py-2 text-sm`
    - Table: `overflow-x-auto` wrapper, `w-full` table
    - Header: Checkbox (select all), Name, Email, Role, Location, Status, Last Active
    - Header checkbox: `aria-label="Select all users"`, toggles all visible user IDs in selectedIds
    - Rows: for each filtered user:
      - Checkbox: `aria-label="Select ${user.displayName}"`, toggles user.uid in selectedIds
      - Name cell: `text-sm font-semibold text-neutral-900` with avatar circle (32px, `bg-brand-600 text-white rounded-full` with first letter of displayName)
      - Email: `text-sm text-neutral-600`
      - Role: capitalize. If null, show "Pending" in italic `text-neutral-400`
      - Location: look up location name from locations data by user.locationId. Show "-" if empty.
      - Status: `<StatusPill status={user.active === false ? "inactive" : user.role === null ? "pending" : "active"} />`
      - Last Active: format user.lastLoginAt as relative date or "Never" if null
      - Row highlight: `bg-brand-50` when `selectedUserId === user.uid`
      - Row click (on the row, not on checkbox): `setSelectedUserId(user.uid)`
    - Loading state: show `<Spinner />` centered
    - Empty state: "No users yet. Users appear here after signing in with Google." at `text-sm text-neutral-500` centered
    - Mutations: `bulkAssignMutation = trpc.admin.bulkAssignRole.useMutation({ onSuccess: () => { toast.success(...); utils.admin.listUsers.invalidate(); setSelectedIds(new Set()); }, onError: (err) => toast.error(err.message) })`
    - Pass `selectedUserId` and `onClose`/`onSave` handlers to UserDetailPanel
    - Render `<UserDetailPanel>` when `selectedUserId !== null`

    **3. UserDetailPanel.tsx** -- User edit side panel (per D-03, D-04, UI-SPEC section 2):
    - Props: `{ userId: string; onClose: () => void }`
    - Data: find user from `trpc.admin.listUsers` cache (or re-query). Use `trpc.admin.listUsers.useQuery()` and find by userId.
    - Locations: `trpc.admin.listAllLocations.useQuery()` for location dropdown
    - Local state: `editRole`, `editLocationId`, `editActive` -- initialized from user data
    - Track dirty state: compare editRole/editLocationId/editActive to original user data
    - On close: if dirty, show `window.confirm("You have unsaved changes. Discard?")`. If confirmed, call onClose. If not, stay.
    - Use `<SidePanel open={true} onClose={handleClose} title={user.displayName} subtitle={user.email}>`
    - Avatar: 64px circle at top, Google photo URL or initials fallback (`bg-brand-600 text-white`)
    - Sections separated by `border-b border-neutral-100 py-4`:
      - **Role assignment**: `<select>` with options: Admin, Driver, Staff. Show current role pre-selected. Use `className="w-full rounded-md border border-neutral-300 px-3 py-2 text-sm"`
      - **Location assignment**: `<select>` with options from locations list. Show current location pre-selected. Add empty option "No location assigned".
      - **Status toggle**: If user is active (active !== false), show "Deactivate User" button with inline double-confirm pattern (armed state, 5s auto-reset per Phase 3 CancelShipmentButton). Initial: `rounded-md border border-red-300 text-red-700 hover:bg-red-50`. Armed: `bg-red-600 text-white`, text "Confirm Deactivate?". If user is inactive, show "Reactivate User" button (single click, `border-neutral-300 text-neutral-700`).
    - Save button: "Save Changes", full-width, `bg-brand-600 text-white hover:bg-brand-700 disabled:bg-neutral-100 disabled:text-neutral-400`. Disabled until dirty.
    - On save: call `updateUserMutation.mutate({ uid: userId, patch: { role: editRole, locationId: editLocationId, active: editActive } })`. On success: toast "User updated", invalidate listUsers, call onClose.
    - Deactivate: set `editActive = false` and auto-save immediately (call mutation directly for deactivation). Reactivate: set `editActive = true` and auto-save immediately.
  </action>
  <verify>
    <automated>cd /Users/quang.nguyen/development/material-tracking && pnpm --filter web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/pages/AdminPage.tsx contains "Admin" heading, AdminTabs component, and conditional tab rendering with activeTab state from URL hash
    - apps/web/src/components/admin/UserTable.tsx contains trpc.admin.listUsers.useQuery, checkbox select-all with `aria-label="Select all users"`, "Assign Role" bulk button, search input with placeholder "Search users..."
    - apps/web/src/components/admin/UserTable.tsx contains trpc.admin.bulkAssignRole.useMutation
    - apps/web/src/components/admin/UserDetailPanel.tsx contains SidePanel wrapper, role select, location select, "Save Changes" button, "Deactivate User" with armed state pattern
    - apps/web/src/components/admin/UserDetailPanel.tsx contains trpc.admin.updateUser.useMutation
    - apps/web/src/components/admin/UserDetailPanel.tsx contains `window.confirm` for unsaved changes
    - `pnpm --filter web exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>AdminPage renders with hash-based tab navigation, Users tab shows table with checkbox selection and bulk role assignment, clicking a row opens UserDetailPanel with role/location editing and deactivation</done>
</task>

</tasks>

<verification>
1. `pnpm --filter web exec tsc --noEmit` -- web package compiles
2. `pnpm --filter web build` -- production build succeeds
3. Visual: navigate to `/admin` as admin -- see Users tab with table
4. Visual: click user row -- side panel slides in from right
5. Visual: sign in as new user (or set role to null in Firestore) -- see PendingApprovalPage
6. Visual: sidebar shows "Admin" only for admin role users
</verification>

<success_criteria>
- AdminPage loads at /admin with four tabs, Users tab active by default
- User table displays all users with searchable filter and checkbox selection
- Bulk role assignment works via toolbar dropdown
- User detail panel slides in on row click with role/location editing
- Deactivate uses double-confirm armed pattern
- PendingApprovalPage renders for null-role users before they reach the app
- Sidebar shows Admin entry for admin users only
- All TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/10-admin-panel-reports/10-02-SUMMARY.md`
</output>
