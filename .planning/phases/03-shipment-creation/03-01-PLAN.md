---
phase: 03-shipment-creation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/enums.ts
  - packages/shared/src/schemas/shipment.ts
  - packages/shared/src/types/shipment.ts
  - packages/shared/src/index.ts
autonomous: true
requirements:
  - SHIP-01
  - INFR-03

must_haves:
  truths:
    - "ShipmentCategory const object includes exactly six string values: documents, parts, samples, equipment, personal, other"
    - "createShipmentSchema requires external receivers to have non-empty name, company, and valid email when isExternal is true"
    - "Shared TypeScript types for ShipmentReceiver include optional company for external contacts"
  artifacts:
    - path: "packages/shared/src/enums.ts"
      provides: "ShipmentCategory aligned with CONTEXT D-05"
      contains: "SAMPLES"
    - path: "packages/shared/src/schemas/shipment.ts"
      provides: "createShipmentSchema + updateShipmentSchema + cancel guard inputs"
      contains: "samples"
    - path: "packages/shared/src/types/shipment.ts"
      provides: "ShipmentReceiver.company optional field"
      contains: "company"
  key_links:
    - from: "packages/shared/src/schemas/shipment.ts"
      to: "packages/shared/src/enums.ts"
      via: "z.enum matching ShipmentCategory values"
---

<objective>
Align shared shipment **enums**, **Zod schemas**, and **TypeScript types** with Phase 3 CONTEXT (D-03, D-05): six categories; external receiver name + company + email; internal directory receiver with `uid` and `isExternal: false`. Add **`updateShipmentInputSchema`** (partial fields allowed for PATCH-style updates) and **`cancelShipmentInputSchema`** (`shipmentId` only) for API use in 03-02.

Purpose: Single source of truth before API and web work (SHIP-01, INFR-03 data contracts).

Output: Updated `enums.ts`, `schemas/shipment.ts`, `types/shipment.ts`; re-exports unchanged in `index.ts` unless new exports added.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-shipment-creation/03-CONTEXT.md
@.planning/phases/03-shipment-creation/03-RESEARCH.md
@packages/shared/src/enums.ts
@packages/shared/src/schemas/shipment.ts
@packages/shared/src/types/shipment.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ShipmentCategory enum — six values</name>
  <files>packages/shared/src/enums.ts</files>
  <read_first>
    - `packages/shared/src/enums.ts` — existing `ShipmentCategory` and `Priority`
    - `packages/shared/src/schemas/shipment.ts` — current `z.enum` for category
  </read_first>
  <action>
    In `packages/shared/src/enums.ts`, replace `ShipmentCategory` const object and type with **exactly** these keys and string values (lowercase, match Zod):

    - `DOCUMENTS: "documents"`
    - `PARTS: "parts"`
    - `SAMPLES: "samples"`
    - `EQUIPMENT: "equipment"`
    - `PERSONAL: "personal"`
    - `OTHER: "other"`

    **Remove** `SUPPLIES` / `"supplies"` entirely.

    Update any JSDoc or comments in the same file if they reference old categories.
  </action>
  <verify>
    <automated>pnpm --filter @material-tracking/shared exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'supplies' packages/shared/src/enums.ts` exits **1** (no matches).
    - `rg -n 'SAMPLES: \"samples\"' packages/shared/src/enums.ts` exits **0**.
    - `rg -n 'PERSONAL: \"personal\"' packages/shared/src/enums.ts` exits **0**.
    - `pnpm --filter @material-tracking/shared exec tsc --noEmit` exits **0**.
  </acceptance_criteria>
  <done>ShipmentCategory reflects CONTEXT D-05 six categories.</done>
</task>

<task type="auto">
  <name>Task 2: createShipmentSchema — category enum + receiver rules</name>
  <files>packages/shared/src/schemas/shipment.ts</files>
  <read_first>
    - `packages/shared/src/schemas/shipment.ts` — full file
    - `packages/shared/src/enums.ts` — after Task 1
  </read_first>
  <action>
    1. Set `category` field to `z.enum(["documents","parts","samples","equipment","personal","other"])` (literal tuple matching Task 1).

    2. Replace `sender` object with:
       - `uid: z.string().min(1)`
       - `name: z.string().min(1)`
       - `email: z.string().email()`
       - `department: z.string().optional()`

    3. Replace `receiver` with a **discriminated union** on `isExternal`:
       - **Internal:** `z.object({ isExternal: z.literal(false), uid: z.string().min(1), name: z.string().min(1), email: z.string().email().optional(), department: z.string().optional() })`
       - **External:** `z.object({ isExternal: z.literal(true), name: z.string().min(1), company: z.string().min(1), email: z.string().email(), department: z.string().optional() })`  
       (No `uid` on external; **company required** per D-03.)

    4. Keep `originId`, `destinationId`, `pieceCount` (int min 1 max 99), `priority`, `description`, `notificationPrefs` as today unless inconsistent with above.

    5. Export **`updateShipmentInputSchema`** — `receiver` is a union, so do **not** use `.partial()` on create alone. Define `updateShipmentInputSchema` as `z.object({ description: z.string().min(1).max(500).optional(), category: z.enum([...same six...]).optional(), priority: z.enum(["urgent","standard","low"]).optional(), sender: (same internal sender object as create).optional(), receiver: z.union([internal receiver, external receiver]).optional(), originId: z.string().min(1).optional(), destinationId: z.string().min(1).optional(), notificationPrefs: createShipmentSchema.shape.notificationPrefs.optional() })` — **omit `pieceCount`** (piece count immutable after create in Phase 3).

    6. Export **`cancelShipmentInputSchema`**: `z.object({ shipmentId: z.string().min(1) })`.

    7. Export TypeScript types: `UpdateShipmentInput`, `CancelShipmentInput` via `z.infer`.
  </action>
  <verify>
    <automated>pnpm --filter @material-tracking/shared exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'z\.enum\\(\\[\"documents\",\"parts\",\"samples\"' packages/shared/src/schemas/shipment.ts` exits **0**.
    - `rg -n 'isExternal: z\\.literal\\(true\\)' packages/shared/src/schemas/shipment.ts` exits **0**.
    - `rg -n 'company: z\\.string\\(\\)\\.min\\(1\\)' packages/shared/src/schemas/shipment.ts` exits **0**.
    - `rg -n 'updateShipmentInputSchema|cancelShipmentInputSchema' packages/shared/src/schemas/shipment.ts` exits **0** with at least two matches.
    - `pnpm --filter @material-tracking/shared exec tsc --noEmit` exits **0**.
  </acceptance_criteria>
  <done>Create/update/cancel inputs are Zod-validated with D-03 external company + email rules.</done>
</task>

<task type="auto">
  <name>Task 3: ShipmentReceiver type — company field</name>
  <files>packages/shared/src/types/shipment.ts</files>
  <read_first>
    - `packages/shared/src/types/shipment.ts` — `ShipmentReceiver`
    - `packages/shared/src/schemas/shipment.ts` — receiver shapes after Task 2
  </read_first>
  <action>
    Update `ShipmentReceiver` interface to match schema intent:
    - Always: `name: string`, `isExternal: boolean`
    - `uid?: string` — present when internal
    - `email?: string` — optional internal; required at runtime for external (enforced by Zod, type as `string | undefined` with comment or use union type if preferred)
    - `department?: string`
    - **`company?: string`** — required when `isExternal === true` at runtime (document in JSDoc)

    If you use a discriminated union type for `ShipmentReceiver`, ensure it exports a single name `ShipmentReceiver` used by `Shipment` interface.
  </action>
  <verify>
    <automated>pnpm --filter @material-tracking/shared exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'company\\?:' packages/shared/src/types/shipment.ts` exits **0**.
    - `pnpm --filter @material-tracking/shared exec tsc --noEmit` exits **0**.
  </acceptance_criteria>
  <done>TS types document external company field for consumers.</done>
</task>

</tasks>

## Verification criteria

- [ ] `pnpm --filter @material-tracking/shared exec tsc --noEmit` passes
- [ ] `rg 'supplies' packages/shared/src` exits 1 or only appears in unrelated strings (must not be ShipmentCategory)

## UAT hints

- N/A (library-only plan)
