---
phase: 03-shipment-creation
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02-PLAN.md
files_modified:
  - apps/web/src/context/AuthContext.tsx
  - apps/web/src/App.tsx
  - apps/web/src/pages/ShipmentCreateStubPage.tsx
  - apps/web/src/pages/ShipmentFormPage.tsx
  - apps/web/src/components/shipment/DirectoryAutocomplete.tsx
  - apps/web/src/components/shipment/PriorityField.tsx
  - apps/web/src/components/shipment/PieceCountStepper.tsx
  - apps/web/src/components/shipment/LocationSelect.tsx
  - apps/web/src/components/shipment/ExternalReceiverFields.tsx
  - apps/web/src/components/shipment/CancelShipmentButton.tsx
autonomous: true
requirements:
  - SHIP-01
  - SHIP-02
  - SHIP-03
  - SHIP-04
  - SHIP-05
  - INFR-03

must_haves:
  truths:
    - "Single scrollable page at /shipments/new submits createShipment via tRPC"
    - "Priority urgent uses left border red-500; standard neutral-300; low slate-400 on the form card"
    - "Cancel uses two-step inline confirm (armed red button then confirm) on edit page only"
  artifacts:
    - path: "apps/web/src/pages/ShipmentFormPage.tsx"
      provides: "Create + edit modes"
      contains: "trpc.shipment"
    - path: "apps/web/src/components/shipment/DirectoryAutocomplete.tsx"
      provides: "Debounced directory.search"
      contains: "300"
  key_links:
    - from: "apps/web/src/App.tsx"
      to: "apps/web/src/pages/ShipmentFormPage.tsx"
      via: "Route paths shipments/new and shipments/:shipmentId/edit"
---

<objective>
Replace the stub with a **tablet-friendly single-page** shipment form (D-04): all fields visible, scroll to submit; **category** select with six options; **priority** with SHIP-03 visual distinction; **piece count** stepper (D-06); **sender/receiver** via `DirectoryAutocomplete` (D-01) with **sender defaulting to current user** (D-02) and **external** branch collecting name + company + email (D-03); **origin/destination** from `locations.list` with **origin default** from profile `locationId` when it matches a listed location (D-11). Add **edit** route `/shipments/:shipmentId/edit` (SHIP-04) and **cancel** with inline double-confirm (D-09, SHIP-05). Extend **`AppUser`** with **`locationId`** loaded from Firestore `users/{uid}`.

Purpose: End-to-end Phase 3 UX; depends on 03-02 API.

Output: New components + routes; remove or repurpose `ShipmentCreateStubPage.tsx` (export can re-export `ShipmentFormPage` for minimal diff).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-shipment-creation/03-CONTEXT.md
@apps/web/src/trpc.tsx
@apps/web/src/App.tsx
@apps/web/src/context/AuthContext.tsx
@apps/web/src/pages/SignInPage.tsx
@packages/shared/src/schemas/shipment.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppUser includes locationId</name>
  <files>apps/web/src/context/AuthContext.tsx</files>
  <read_first>
    - `apps/web/src/context/AuthContext.tsx` — `AppUser`, `loadProfile`, `setAppUser`
    - `packages/shared/src/schemas/user.ts` — `locationId`
  </read_first>
  <action>
    1. Add `locationId: string` to `AppUser` interface.

    2. When reading `userDoc.data()`, set `locationId: (data.locationId as string) ?? ""` in `setAppUser` object.

    3. Keep backward compatibility: default `""` when missing.
  </action>
  <verify>
    <automated>pnpm --filter @material-tracking/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'locationId' apps/web/src/context/AuthContext.tsx` exits **0** with at least **2** matches.
    - `pnpm --filter @material-tracking/web exec tsc --noEmit` exits **0**.
  </acceptance_criteria>
  <done>Web can default origin from profile (D-11).</done>
</task>

<task type="auto">
  <name>Task 2: DirectoryAutocomplete component</name>
  <files>apps/web/src/components/shipment/DirectoryAutocomplete.tsx</files>
  <read_first>
    - `apps/web/src/trpc.tsx` — `trpc` usage
    - `apps/web/src/context/AuthContext.tsx` — for optional disabled state
  </read_first>
  <action>
    Create controlled component with props:
    - `label: string`, `value: { uid?: string; name: string; email: string; department?: string; isExternal: boolean; company?: string } | null`
    - `onChange: (v) => void`
    - `onExternalToggle: (external: boolean) => void` **or** internal toggle UI **“External contact”** checkbox

    Behavior:
    - When **not** external: text input with **`debounce 300ms`** (use `useEffect` + timeout or `useDeferredValue` — acceptance: source contains **`300`**) before calling `trpc.directory.search.useQuery({ query }, { enabled: query.length >= 2 && !external })` — adjust to match react-query v5 API.
    - Render dropdown list of results; on select, set internal value with `isExternal: false`, `uid`, `name`, `email`, `department`.
    - When **external**: show `ExternalReceiverFields` (Task 3) instead of directory search; value `isExternal: true` with `company` required.

    Accessibility: associate `label` with input via `htmlFor` / `id`.
  </action>
  <verify>
    <automated>pnpm --filter @material-tracking/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/src/components/shipment/DirectoryAutocomplete.tsx` exits **0**.
    - `rg -n 'directory\\.search|directory\\.' apps/web/src/components/shipment/DirectoryAutocomplete.tsx` exits **0**.
    - `rg -n '300' apps/web/src/components/shipment/DirectoryAutocomplete.tsx` exits **0**.
    - `pnpm --filter @material-tracking/web exec tsc --noEmit` exits **0**.
  </acceptance_criteria>
  <done>SHIP-02 autocomplete path wired to tRPC.</done>
</task>

<task type="auto">
  <name>Task 3: ExternalReceiverFields, PriorityField, PieceCountStepper, LocationSelect</name>
  <files>apps/web/src/components/shipment/ExternalReceiverFields.tsx, apps/web/src/components/shipment/PriorityField.tsx, apps/web/src/components/shipment/PieceCountStepper.tsx, apps/web/src/components/shipment/LocationSelect.tsx</files>
  <read_first>
    - `packages/shared/src/schemas/shipment.ts` — field constraints
  </read_first>
  <action>
    1. **ExternalReceiverFields:** inputs for `name`, `company`, `email` (all required, email type email).

    2. **PriorityField:** radio group or segmented control for `urgent` | `standard` | `low`. Wrap fieldset in container `div` with `data-priority={value}` and classes:
       - `urgent`: `border-l-4 border-red-500 pl-3`
       - `standard`: `border-l-4 border-neutral-300 pl-3`
       - `low`: `border-l-4 border-slate-400 pl-3`

    3. **PieceCountStepper:** number input min 1 max 99 plus `-` / `+` buttons calling `onChange` with clamped int.

    4. **LocationSelect:** `label`, `value` (locationId string), `options` from `trpc.locations.list.useQuery()`, `onChange`. Options sorted by `name`. If loading, show disabled select with placeholder **"Loading locations…"**.
  </action>
  <verify>
    <automated>pnpm --filter @material-tracking/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'border-red-500' apps/web/src/components/shipment/PriorityField.tsx` exits **0**.
    - `rg -n 'border-neutral-300' apps/web/src/components/shipment/PriorityField.tsx` exits **0**.
    - `rg -n 'border-slate-400' apps/web/src/components/shipment/PriorityField.tsx` exits **0**.
    - `rg -n 'locations\\.list' apps/web/src/components/shipment/LocationSelect.tsx` exits **0**.
    - `pnpm --filter @material-tracking/web exec tsc --noEmit` exits **0**.
  </acceptance_criteria>
  <done>Reusable field blocks match D-05/D-06/SHIP-03/INFR-03.</done>
</task>

<task type="auto">
  <name>Task 4: CancelShipmentButton (inline double confirm)</name>
  <files>apps/web/src/components/shipment/CancelShipmentButton.tsx</files>
  <read_first>
    - `03-CONTEXT.md` — D-09
  </read_first>
  <action>
    Implement button that:
    - **First click:** sets local `armed` state true; button class includes **`bg-red-600`** (or `destructive` red background) and label changes to **"Click again to cancel"** (exact substring).
    - **Second click** within **5 seconds** of arming: calls `trpc.shipment.cancel.mutate({ shipmentId })`, `onSuccess` navigate to `/dashboard` or show toast.
    - If **5 seconds** elapse without second click, reset `armed` to false (use `setTimeout` / cleanup).
    - Disable if mutation is pending.

    Acceptance must grep for **`Click again to cancel`** and **`5000`** or **`5 * 1000`** timeout.
  </action>
  <verify>
    <automated>pnpm --filter @material-tracking/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Click again to cancel' apps/web/src/components/shipment/CancelShipmentButton.tsx` exits **0**.
    - `rg -n 'cancel\\.mutate|shipment\\.cancel' apps/web/src/components/shipment/CancelShipmentButton.tsx` exits **0**.
    - `rg -n '5000|5 \\* 1000' apps/web/src/components/shipment/CancelShipmentButton.tsx` exits **0**.
    - `pnpm --filter @material-tracking/web exec tsc --noEmit` exits **0**.
  </acceptance_criteria>
  <done>D-09 cancel UX implemented.</done>
</task>

<task type="auto">
  <name>Task 5: ShipmentFormPage + routes</name>
  <files>apps/web/src/pages/ShipmentFormPage.tsx, apps/web/src/App.tsx, apps/web/src/pages/ShipmentCreateStubPage.tsx</files>
  <read_first>
    - `apps/web/src/App.tsx` — routes
    - `apps/web/src/pages/ShipmentCreateStubPage.tsx`
    - Components from Tasks 2–4
  </read_first>
  <action>
    1. Add **`ShipmentFormPage`** with props `mode: "create" | "edit"` from router (**use `useParams`**: `shipmentId` optional — create when path is `new`, edit when `shipmentId` present).

    2. **`create` mode:**
       - Initialize sender from `useAuthContext()`: `uid` from `user.uid`, `name` from `appUser?.displayName`, `email` from `appUser?.email` — require `user` non-null (page behind `AuthGate`).
       - Initialize `originId` from `appUser?.locationId` if that id exists in `locations.list` data; else first active location or empty with validation error on submit.
       - Submit: `trpc.shipment.create.mutate` with `createShipmentInput` built from form state; on success `navigate(\`/shipments/${id}/edit\`)` **or** `/dashboard` — **concrete:** navigate to **`/shipments/${data.shipmentId}/edit`**.

    3. **`edit` mode:**
       - `trpc.shipment.getById.useQuery({ shipmentId })` — loading/error states.
       - Pre-fill form; if `data.status !== "created"`, show read-only banner **"This shipment can no longer be edited."** (exact string) and disable inputs **except** no cancel if not created? — **Concrete:** if status is `cancelled` or not `created`, hide save and cancel actions; show banner.

    4. Save: `trpc.shipment.update.mutate({ shipmentId, patch })`.

    5. Render **CancelShipmentButton** only in edit mode when `status === "created"`.

    6. **`App.tsx`:** Replace `ShipmentCreateStubPage` on `/shipments/new` with `<ShipmentFormPage />` wrapper or element that sets mode create — e.g. `<ShipmentFormPage mode="create" />` using separate route components:
       - `path="shipments/new"` → `<ShipmentFormPage />` with internal `mode=create`
       - `path="shipments/:shipmentId/edit"` → `<ShipmentFormPage />` with `mode=edit`

    7. **`ShipmentCreateStubPage.tsx`:** Either delete and update imports to `ShipmentFormPage` only, or re-export `ShipmentFormPage` as `ShipmentCreateStubPage` for minimal churn — prefer **update `App.tsx` imports** to `ShipmentFormPage` and **remove** stub file **or** keep stub as one-line re-export: `export { ShipmentFormPage as ShipmentCreateStubPage } from "./ShipmentFormPage"` — **Concrete:** `App.tsx` imports **`ShipmentFormPage`** from `./pages/ShipmentFormPage`.

    8. Form fields: `description` textarea, `category` select with options **Documents, Parts, Samples, Equipment, Personal, Other** (display labels) mapping to enum strings **documents, parts, samples, equipment, personal, other**.

    9. Sticky footer optional; minimum: submit button at bottom inside scrollable column (`flex flex-col min-h-0` on tablet).
  </action>
  <verify>
    <automated>pnpm --filter @material-tracking/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'shipments/:shipmentId/edit' apps/web/src/App.tsx` exits **0**.
    - `rg -n 'ShipmentFormPage' apps/web/src/App.tsx` exits **0**.
    - `rg -n 'shipment\\.create\\.mutate|shipment\\.update\\.mutate' apps/web/src/pages/ShipmentFormPage.tsx` exits **0**.
    - `rg -n 'This shipment can no longer be edited\\.' apps/web/src/pages/ShipmentFormPage.tsx` exits **0**.
    - `rg -n 'Documents|Samples|Personal' apps/web/src/pages/ShipmentFormPage.tsx` exits **0**.
    - `pnpm --filter @material-tracking/web exec tsc --noEmit` exits **0**.
  </acceptance_criteria>
  <done>SHIP-01/03/04/05 and D-04 satisfied in UI.</done>
</task>

</tasks>

## Verification criteria

- [ ] `pnpm --filter @material-tracking/web exec tsc --noEmit` passes
- [ ] Manual: create flow with `DIRECTORY_STUB=1` API env

## UAT hints

- Sign in as staff; open `/shipments/new`; verify sender pre-filled; change origin; submit; land on edit; cancel with double confirm; verify Firestore `status: cancelled`.
