---
phase: 09-history-search-audit
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/web/src/pages/HistoryPage.tsx
  - apps/web/src/App.tsx
  - apps/web/src/components/layout/Sidebar.tsx
  - apps/web/src/components/dashboard/ShipmentTable.tsx
autonomous: true
requirements:
  - HIST-01

must_haves:
  truths:
    - "Authenticated staff can open /history and see a filter bar and shipment results"
    - "Date range and status narrow results via shipment.search; sender, receiver, and description keyword filter the current loaded set with AND logic (per D-02/D-03)"
    - "Load More fetches the next cursor page (50 per page from API)"
    - "Sidebar includes History; route is registered in App.tsx"
  artifacts:
    - path: "apps/web/src/pages/HistoryPage.tsx"
      provides: "History page UI, filters, table, pagination"
      exports: ["HistoryPage"]
    - path: "apps/web/src/App.tsx"
      provides: "/history route"
    - path: "apps/web/src/components/layout/Sidebar.tsx"
      provides: "History nav item"
  key_links:
    - from: "HistoryPage"
      to: "trpc.shipment.search"
      via: "useQuery or useInfiniteQuery with cursor"
      pattern: "shipment\\.search"
    - from: "HistoryPage"
      to: "ShipmentTable"
      via: "reuse or extend props for history mode"
      pattern: "ShipmentTable"
---

<objective>
Ship the `/history` experience: filter bar (date from/to, status, sender, receiver, keyword), results table following dashboard `ShipmentTable` patterns, Load More cursor pagination, and navigation entry.

Purpose: Delivers HIST-01 end-to-end for staff per **D-01**–**D-04**. Depends on Plan 09-01 for `shipment.search`.

Output: `HistoryPage`, route wiring, sidebar link, and minimal `ShipmentTable` adjustments if needed for reuse.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-history-search-audit/09-CONTEXT.md

@apps/web/src/components/dashboard/ShipmentTable.tsx
@apps/web/src/components/dashboard/StatusBadge.tsx
@apps/web/src/trpc.tsx
@apps/web/src/App.tsx
@apps/web/src/components/layout/Sidebar.tsx

<interfaces>
From `apps/web/src/components/dashboard/ShipmentTable.tsx`:
```typescript
export type SortField = "priority" | "status" | "createdAt" | "updatedAt";
export type SortDirection = "asc" | "desc";
export function ShipmentTable(props: {
  shipments: ShipmentWithId[];
  exceptionsMap: Map<string, ExceptionType[]>;
  onRowClick: (shipmentId: string) => void;
  sortField: SortField;
  sortDirection: SortDirection;
  onSort: (field: SortField) => void;
}): JSX.Element;
export function sortShipments(shipments: ShipmentWithId[], field: SortField, direction: SortDirection): ShipmentWithId[];
```

History page may pass an empty `exceptionsMap` or reuse `classifyAllExceptions` if imported types match — **Claude's discretion** per CONTEXT.

tRPC React Query: follow existing patterns in `ShipmentFormPage` / dashboard for `trpc.shipment.*.useQuery`.

Sidebar: add nav item with `roles: ["staff", "admin"]` (same as New Shipment) unless product requires drivers to view history — default staff+admin per **D-01** dedicated history for operational search.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HistoryPage — filters, search, table, Load More</name>
  <read_first>
    - `apps/web/src/pages/DashboardPage.tsx` — table + state patterns.
    - `apps/web/src/components/dashboard/ShipmentTable.tsx` — props and sorting helpers.
    - Plan **09-01** / shared schema — exact `shipment.search` input and output names.
    - `09-CONTEXT.md` **D-02**, **D-03** — AND filters; client-side text filtering on loaded rows.
  </read_first>
  <files>apps/web/src/pages/HistoryPage.tsx, apps/web/src/components/dashboard/ShipmentTable.tsx</files>
  <action>
    Create `HistoryPage.tsx`:
    - Filter controls: `dateFrom`, `dateTo` (native `<input type="date">` or datetime-local per discretion), status `<select>` including "All", sender and receiver text inputs, description keyword input.
    - On Apply (or debounced sync — choose one, prefer explicit Apply to avoid query storms), call `shipment.search` with date range + status; accumulate pages in state or use TanStack Query infinite query with `getNextPageParam` from `nextCursor`.
    - **Client-side AND filters** on the union of loaded `items`: match sender string against `sender.name`/`email`, receiver against `receiver.name`/`email`/`company`, keyword against `description` (case-insensitive substring). Apply after each page fetch to the combined list (document in comment that very large histories may need v2 search service per **D-04**).

    Reuse `ShipmentTable` and `sortShipments` where possible. If `ShipmentWithId` typing differs from API response, map fields in one place. Row click navigates to `/shipments/:id`.

    Empty state: copy for no results (deferred export to Phase 10 per CONTEXT).

    Add `Load More` button visible when `hasMore`; disabled while fetching.

    Optional small refactor to `ShipmentTable` only if required for reuse (e.g. optional column visibility) — keep diff minimal.
  </action>
  <acceptance_criteria>
    - All five filter dimensions from **D-02** are present; text filters apply with AND logic on loaded data.
    - Pagination uses API cursor and 50-per-page contract from 09-01.
    - Table UX matches dashboard patterns (badges, sortable columns as appropriate).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/quang.nguyen/development/material-tracking && pnpm --filter web build</automated>
  </verify>
  <done>History page builds and wires tRPC search + table + Load More.</done>
</task>

<task type="auto">
  <name>Task 2: Route and Sidebar History link</name>
  <read_first>
    - `apps/web/src/App.tsx` — `RequireRole` patterns for staff routes.
    - `apps/web/src/components/layout/Sidebar.tsx` — `navItems` structure.
  </read_first>
  <files>apps/web/src/App.tsx, apps/web/src/components/layout/Sidebar.tsx</files>
  <action>
    Register `/history` under `AppLayout` with the same role gate as other staff shipment views (`staff` + `admin`).

    Add Sidebar entry "History" with an appropriate icon (emoji consistent with existing sidebar style per **Claude's discretion** in CONTEXT). Path `/history`.

    Ensure default index redirect behavior unchanged unless product wants `/` → history (do not change without explicit requirement).
  </action>
  <acceptance_criteria>
    - `/history` loads `HistoryPage` for allowed roles; others see access denied or redirect per existing `RequireRole` behavior.
    - Sidebar shows History for the same roles that can open the page.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/quang.nguyen/development/material-tracking && pnpm --filter web build</automated>
  </verify>
  <done>Route and nav link committed and typecheck clean.</done>
</task>

</tasks>

<verification>
- `pnpm --filter web build` succeeds.
- Manual smoke: open `/history`, apply filters, Load More (checkpoint optional if automated E2E not present).
</verification>

<success_criteria>
- HIST-01 satisfied for UI: searchable history with all filter dimensions and pagination per locked decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/09-history-search-audit/09-02-SUMMARY.md`
</output>
