---
phase: 09-history-search-audit
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/web/src/components/shipment/ShipmentTimeline.tsx
  - apps/web/src/components/shipment/PieceEventsList.tsx
  - apps/web/src/pages/ShipmentFormPage.tsx
autonomous: true
requirements:
  - HIST-03
  - ADMN-05

must_haves:
  truths:
    - "Shipment detail shows a vertical timeline with connected nodes, chronological order, color by action type"
    - "Entries include icon, relative + absolute time, user name, piece number for scan events, and signature/photo indicators when present"
    - "Timeline includes shipment creation, all piece scan events, and cancellation when applicable (per D-05–D-07, D-09–D-10)"
  artifacts:
    - path: "apps/web/src/components/shipment/ShipmentTimeline.tsx"
      provides: "Visual timeline replacing list-only UI"
      exports: ["ShipmentTimeline"]
    - path: "apps/web/src/pages/ShipmentFormPage.tsx"
      provides: "Wires timeline with shipment + pieces data"
  key_links:
    - from: "ShipmentTimeline"
      to: "Piece.events + shipment metadata"
      via: "merged sorted event model built in component or small util"
      pattern: "events"
---

<objective>
Replace the flat `PieceEventsList` on shipment detail with a **ShipmentTimeline**: vertical connected dots, action-colored, chronological (oldest → top or bottom — pick one convention and apply consistently), showing creation, scans, and cancellation as an auditable trail.

Purpose: **HIST-03** (full timeline with who/when) and **ADMN-05** via **D-09**–**D-10** (audit derived from existing Firestore fields, no new audit collection).

Output: New component integrated on read-only detail view; `PieceEventsList` may be deleted or reduced to a thin re-export if still referenced elsewhere.

**Execution note:** `depends_on: ["09-01"]` is for wave ordering only (run after API wave). Timeline work does not call `shipment.search`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-history-search-audit/09-CONTEXT.md

@apps/web/src/components/shipment/PieceEventsList.tsx
@apps/web/src/pages/ShipmentFormPage.tsx
@packages/shared/src/types/piece.ts
@packages/shared/src/types/shipment.ts
@packages/shared/src/enums.ts

<interfaces>
`PieceEvent` (shared): `action`, `timestamp`, `userId`, `userName`, optional `location`, `signatureUrl`, `photoUrls[]`.

`Shipment`: `createdBy`, `createdAt`, `status`, `updatedAt` — use for synthetic **Created** and **Cancelled** entries when `status === 'cancelled'` (cancellation time approximate `updatedAt` per **D-09**; document limitation in component comment if no dedicated cancel actor field).

Scan actions: `ScanAction` / piece status labels from enums — map colors per **D-05**: created neutral, in_transit blue, delivered green, picked_up purple, cancelled red.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ShipmentTimeline component</name>
  <read_first>
    - `PieceEventsList.tsx` — timestamp parsing, action labels (extend for cancelled if missing).
    - `09-CONTEXT.md` **D-05**–**D-07** — layout and fields.
  </read_first>
  <files>apps/web/src/components/shipment/ShipmentTimeline.tsx, apps/web/src/components/shipment/PieceEventsList.tsx</files>
  <action>
    Implement `ShipmentTimeline` accepting:
    - Shipment-level props: `createdBy`, `createdAt`, `status`, `updatedAt` (types aligned with `getById` response / form state).
    - Pieces: `Array<{ pieceNumber: number; events: PieceEventLike[] }>` where `PieceEventLike` includes optional `signatureUrl` and `photoUrls` for **D-07** indicators.

    Build a single merged timeline array:
    1. One **Shipment created** event at `createdAt` with `createdBy.name`.
    2. All piece events with `pieceNumber` on each row; sort by `timestamp` ascending (or descending — pick ascending for "story from start to end" and state in SUMMARY).
    3. If `status === 'cancelled'`, append **Cancelled** at `updatedAt` with note that actor is system/unknown if not stored (**D-06** / **D-09**).

    Visual design: vertical line connecting dots; dot or icon color per action; show `formatDistanceToNow` + absolute timestamp (use `date-fns` if already in project; else small inline formatter matching `PieceEventsList` style).

    **D-07**: Show small icons or text badges for signature/photo attachments on scan rows when URLs exist.

    Remove or deprecate `PieceEventsList` implementation — either delete file and update imports, or re-export `ShipmentTimeline` as `PieceEventsList` only if needed for transitional compatibility (prefer clean rename).

    **Claude's discretion**: subtle CSS transitions for timeline — CONTEXT allows; keep accessible (contrast, not motion-only).
  </action>
  <acceptance_criteria>
    - Meets **D-05**–**D-07** visually and structurally.
    - Chronological order is consistent and tested by manual or unit test where feasible.
    - Cancellation and creation appear when applicable.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/quang.nguyen/development/material-tracking && pnpm --filter web build</automated>
  </verify>
  <done>Component file exists and builds; `PieceEventsList` no longer used for detail scan history or is aliased without duplicate UI.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ShipmentFormPage detail section</name>
  <read_first>
    - `ShipmentFormPage.tsx` — `piecesQuery`, shipment fields available in read-only detail mode.
  </read_first>
  <files>apps/web/src/pages/ShipmentFormPage.tsx</files>
  <action>
    Replace `PieceEventsList` usage with `ShipmentTimeline`. Pass full event objects including `signatureUrl` and `photoUrls` from Firestore (stop stripping fields in the `.map` that builds events).

    Update section heading if needed ("Scan History" → "Activity" or keep — minor copy discretion).

    Ensure loading/error states still behave when pieces are loading.
  </action>
  <acceptance_criteria>
    - Detail view shows timeline with enriched events.
    - No regression for create/edit modes (timeline only in read-only detail path as today).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/quang.nguyen/development/material-tracking && pnpm --filter web build</automated>
  </verify>
  <done>Shipment detail uses `ShipmentTimeline` with complete event payload.</done>
</task>

</tasks>

<verification>
- `pnpm --filter web build` passes.
- Visual check recommended: one shipment with scans + optional signature/photos + cancelled shipment (checkpoint:human-verify optional post-execute).
</verification>

<success_criteria>
- **HIST-03**: Users see a full, readable timeline on the shipment detail page.
- **ADMN-05**: Audit trail is visible via timeline (**D-10**) without a separate audit store (**D-09**).
</success_criteria>

<output>
After completion, create `.planning/phases/09-history-search-audit/09-03-SUMMARY.md`
</output>
