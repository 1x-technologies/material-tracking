---
phase: 09-history-search-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/schemas/shipment-search.ts
  - packages/shared/src/index.ts
  - apps/api/src/routers/shipment.ts
  - apps/api/tests/shipment-router.test.ts
  - firestore.indexes.json
autonomous: true
requirements:
  - HIST-01
  - HIST-02

must_haves:
  truths:
    - "Staff can fetch paginated shipment rows filtered by createdAt range and optional status via tRPC"
    - "Each page returns at most 50 shipments plus a cursor when more data exists"
    - "No codebase change introduces shipment TTL, scheduled purge, or auto-delete of history"
  artifacts:
    - path: "apps/api/src/routers/shipment.ts"
      provides: "shipment.search query"
      contains: "search:"
    - path: "packages/shared/src/schemas/shipment-search.ts"
      provides: "Zod input + exported types for search"
    - path: "apps/api/tests/shipment-router.test.ts"
      provides: "Automated tests for search pagination and filters"
  key_links:
    - from: "shipment.search input"
      to: "Firestore shipments query"
      via: "where/orderBy/startAfter/limit(51)"
      pattern: "collection\\('shipments'\\)"
    - from: "cursor in response"
      to: "next page request"
      via: "client passes cursor back as input"
      pattern: "cursor"
---

<objective>
Add `shipment.search` on the API: Firestore-backed listing with optional status filter, `createdAt` date range, and cursor-based pagination (50 items per page, request 51 to detect `hasMore`).

Purpose: HIST-01 needs a server query that respects D-02/D-03 (composite filters on status + date; sender/receiver/keyword stay client-side in Plan 09-02). HIST-02 is satisfied by policy (no purge); this plan locks that in with an explicit verification pass.

Output: Shared Zod schema, router procedure, tests, and any required Firestore index entries.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-history-search-audit/09-CONTEXT.md

@apps/api/src/routers/shipment.ts
@apps/api/src/middleware/auth.ts
@firestore.indexes.json

<interfaces>
Existing `shipmentRouter` uses `staffProcedure` for mutations/listing patterns; `getById` uses `staffProcedure`. Follow the same auth level as other staff-only shipment reads unless product requires `protectedProcedure` — default to **staffProcedure** to match `getById` (adjust only if REQUIREMENTS or CONTEXT explicitly widen access).

Firestore already defines a composite index on `shipments`: `status` ASC + `createdAt` DESC (see `firestore.indexes.json`). For queries that filter only by `createdAt` range with `orderBy('createdAt','desc')`, single-field rules apply; if you add a second `orderBy` on document ID for stable cursors, add the matching composite index when the emulator or deploy error requests it.

Shipment document fields (from `packages/shared/src/types/shipment.ts`): `status`, `createdAt`, `sender`, `receiver`, `description`, `shipmentNumber`, etc. Return shape should align with dashboard consumption: `{ id, ...doc.data() }` serializable to JSON (Timestamps as Firestore admin Timestamp or ISO strings — match existing `getById` client expectations).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: shipment.search schema and procedure</name>
  <read_first>
    - `apps/api/src/routers/shipment.ts` — procedure patterns, `db` usage, error codes.
    - `packages/shared/src/index.ts` — how schemas are exported.
    - `firestore.indexes.json` — existing `shipments` indexes.
    - `09-CONTEXT.md` **D-03** — Firestore for status + createdAt; text filters are client-side only in Phase 9 web plan.
  </read_first>
  <files>packages/shared/src/schemas/shipment-search.ts, packages/shared/src/index.ts, apps/api/src/routers/shipment.ts, firestore.indexes.json</files>
  <action>
    Add `shipmentSearchInputSchema` in shared package: optional ISO date strings `dateFrom`, `dateTo` (interpret as UTC day boundaries or explicit instants — document choice in code comments); optional `status` enum matching `ShipmentStatus` or omit/`undefined` meaning all statuses; optional opaque `cursor` object carrying values needed for `startAfter` (e.g. last `createdAt` + `id`).

    Implement `search: staffProcedure.input(shipmentSearchInputSchema).query` that:
    - Builds `shipments` query with `orderBy('createdAt','desc')`, `limit(51)`.
    - Applies `where('status','==', input.status)` when status is provided (per **D-03**).
    - Applies `createdAt` range filters when dates provided (use Firestore Timestamp from parsed ISO).
    - Applies `startAfter` when cursor present; ensure stable ordering (add `orderBy(FieldPath.documentId(), 'desc')` if needed and add index to `firestore.indexes.json`).

    Response: `{ items: ShipmentRow[], nextCursor: Cursor | null, hasMore: boolean }` where returned `items` has at most 50 elements; `hasMore` true when 51 docs read (then slice to 50). Do not implement sender/receiver/keyword in this procedure (**D-03**).

    Avoid drive-by refactors elsewhere in the router.
  </action>
  <acceptance_criteria>
    - Input validated with Zod; invalid dates return `BAD_REQUEST`.
    - Status + date filters combine with AND semantics.
    - Page size is 50 for returned items; cursor allows fetching the next page without duplicates.
    - Procedure is authenticated and consistent with other shipment read procedures.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/quang.nguyen/development/material-tracking/apps/api && pnpm exec vitest run tests/shipment-router.test.ts --reporter=dot</automated>
  </verify>
  <done>`shipment.search` exists, compiles, and tests cover happy path + cursor + status + date bounds (mock Firestore chain as existing tests do).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tests and HIST-02 retention check</name>
  <read_first>
    - `apps/api/tests/shipment-router.test.ts` — mock `db.collection` / query chain patterns.
    - `.planning/phases/09-history-search-audit/09-CONTEXT.md` **D-08** — no auto-purge.
  </read_first>
  <files>apps/api/tests/shipment-router.test.ts</files>
  <action>
    Extend Vitest: call `shipment.search` with mocked query snapshots — assert `limit` called with 51, `hasMore` logic, and cursor pass-through on second call.

    HIST-02 (**D-08**): Grep repo for TTL / `expireAt` / scheduled delete of `shipments` (Cloud Functions cron, `delete()`, etc.). If none found, add a short comment in `shipment.search` or shared schema file: "Shipment documents are retained indefinitely; no server-side purge (HIST-02)." Do not add TTL fields or deletion logic.
  </action>
  <acceptance_criteria>
    - New tests fail if pagination or filter wiring regresses.
    - No new purge/TTL behavior; documentation comment exists for auditors.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/quang.nguyen/development/material-tracking/apps/api && pnpm exec vitest run tests/shipment-router.test.ts --reporter=dot</automated>
  </verify>
  <done>All shipment router tests pass; HIST-02 explicitly acknowledged in code or plan summary.</done>
</task>

</tasks>

<verification>
- `pnpm exec vitest run tests/shipment-router.test.ts` passes.
- Deploy or emulator: if Firestore returns missing-index error for the chosen query shape, `firestore.indexes.json` is updated and documented in SUMMARY.
</verification>

<success_criteria>
- tRPC `shipment.search` returns paginated, filterable shipment history aligned with **D-03**.
- HIST-01 backend slice and HIST-02 retention policy are evidenced in code/tests.
</success_criteria>

<output>
After completion, create `.planning/phases/09-history-search-audit/09-01-SUMMARY.md`
</output>
