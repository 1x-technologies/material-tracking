---
phase: 05-scan-processing-status-workflow
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01-PLAN.md
files_modified:
  - apps/web/package.json
  - apps/web/src/components/scan/scanSounds.ts
  - apps/web/src/components/scan/ActionSelector.tsx
  - apps/web/src/components/scan/ScanInput.tsx
  - apps/web/src/components/scan/CameraScanOverlay.tsx
  - apps/web/src/components/scan/ScannedPiecesList.tsx
  - apps/web/src/pages/ScanPage.tsx
  - apps/web/src/pages/ScanStubPage.tsx
  - apps/web/src/App.tsx
  - apps/web/src/components/layout/AppLayout.tsx
autonomous: true
requirements: [SCAN-01, SCAN-02]
must_haves:
  truths:
    - "User can scan via RF scanner: auto-focused input, keyboard wedge types QR + Enter submits"
    - "User can scan via camera: button opens viewfinder overlay, decoded QR auto-submits"
    - "User selects target action before scanning (In Transit / Delivered / Completed)"
    - "Success toast and accumulating card list appear after each valid scan"
    - "Inline red error appears below input on scan failure, clears on next attempt"
    - "Audio beep on success, buzz on failure"
    - "Any authenticated user can access /scan (not restricted to driver/admin)"
  artifacts:
    - path: "apps/web/src/pages/ScanPage.tsx"
      provides: "Main scan page composing all scan components"
      min_lines: 60
    - path: "apps/web/src/components/scan/ScanInput.tsx"
      provides: "Auto-focused text input with Enter-to-submit for RF scanner"
      exports: ["ScanInput"]
    - path: "apps/web/src/components/scan/ActionSelector.tsx"
      provides: "Segmented control for scan action selection"
      exports: ["ActionSelector"]
    - path: "apps/web/src/components/scan/CameraScanOverlay.tsx"
      provides: "Camera viewfinder overlay using html5-qrcode"
      exports: ["CameraScanOverlay"]
    - path: "apps/web/src/components/scan/ScannedPiecesList.tsx"
      provides: "Accumulating session list of scanned pieces"
      exports: ["ScannedPiecesList"]
    - path: "apps/web/src/components/scan/scanSounds.ts"
      provides: "Web Audio API helpers for success/error feedback"
      exports: ["playSuccessBeep", "playErrorBuzz"]
  key_links:
    - from: "apps/web/src/pages/ScanPage.tsx"
      to: "trpc.scan.process"
      via: "tRPC mutation call on scan submit"
      pattern: "trpc\\.scan\\.process"
    - from: "apps/web/src/pages/ScanPage.tsx"
      to: "apps/web/src/components/scan/ScanInput.tsx"
      via: "onScan callback prop"
      pattern: "ScanInput"
    - from: "apps/web/src/App.tsx"
      to: "apps/web/src/pages/ScanPage.tsx"
      via: "Route element without RequireRole wrapper"
      pattern: "ScanPage"
---

<objective>
Build the scan page UI: action selector (D-03), RF scanner text input (D-01), camera fallback overlay (D-02), audio feedback (D-09), toast notifications (D-07), inline errors (D-08), and accumulating scanned-pieces list. Replace ScanStubPage and widen /scan route access to any authenticated user (D-05).

Purpose: This is the driver-facing interface where all scanning happens.
Output: ScanPage + 5 scan components + updated App.tsx route + Sonner Toaster in layout
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-scan-processing-status-workflow/05-CONTEXT.md
@.planning/phases/05-scan-processing-status-workflow/05-RESEARCH.md
@.planning/phases/05-scan-processing-status-workflow/05-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 that this plan consumes via tRPC -->

From packages/shared/src/types/scan.ts:
```typescript
export interface ScanResult {
  pieceId: string;
  shipmentId: string;
  newStatus: string;
  shipmentNumber: string;
  pieceNumber: number;
}
```

From packages/shared/src/enums.ts:
```typescript
export const ScanAction = {
  CREATED: "created", IN_TRANSIT: "in_transit",
  DELIVERED: "delivered", COMPLETED: "completed",
} as const;
export type ScanAction = (typeof ScanAction)[keyof typeof ScanAction];
```

From apps/web/src/trpc.tsx:
```typescript
export const trpc = createTRPCReact<AppRouter>();
// Usage: trpc.scan.process.useMutation()
```

From apps/web/src/App.tsx (current /scan route to modify):
```tsx
<Route path="scan" element={
  <RequireRole allowedRoles={["driver", "admin"]}>
    <ScanStubPage />
  </RequireRole>
} />
```

From apps/web/src/components/layout/AppLayout.tsx:
```tsx
export function AppLayout() {
  return (
    <div className="min-h-screen bg-white">
      <TopBar />
      <div className="flex h-[calc(100vh-4rem)]">
        <Sidebar />
        <main className="flex-1 overflow-y-auto p-4 lg:p-6">
          <div className="mx-auto max-w-[--max-width-container]">
            <Outlet />
          </div>
        </main>
      </div>
    </div>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install deps + create scan utility components</name>
  <files>apps/web/package.json, apps/web/src/components/scan/scanSounds.ts, apps/web/src/components/scan/ActionSelector.tsx, apps/web/src/components/scan/ScanInput.tsx, apps/web/src/components/scan/CameraScanOverlay.tsx, apps/web/src/components/scan/ScannedPiecesList.tsx</files>
  <read_first>
    - apps/web/package.json (verify html5-qrcode and sonner are NOT yet installed)
    - packages/shared/src/enums.ts (ScanAction values for ActionSelector)
    - packages/shared/src/types/scan.ts (ScanResult for ScannedPiecesList)
  </read_first>
  <action>
    1. Run `pnpm --filter @material-tracking/web add html5-qrcode sonner`

    2. Create `apps/web/src/components/scan/scanSounds.ts`:
    - Export `playSuccessBeep()`: create AudioContext, oscillator at 440Hz (sine wave), gain 0.08, play for 150ms. Handle AudioContext `state === "suspended"` by calling `ctx.resume()` before playing.
    - Export `playErrorBuzz()`: create AudioContext, oscillator at 200Hz (square wave), gain 0.08, play for 300ms. Same suspend handling.
    - Reuse a module-level AudioContext instance to avoid creating new contexts on every call.

    3. Create `apps/web/src/components/scan/ActionSelector.tsx`:
    - Props: `{ value: string; onChange: (action: string) => void }`
    - Define actions array: `[{ value: "in_transit", label: "In Transit" }, { value: "delivered", label: "Delivered" }, { value: "completed", label: "Completed" }]`
    - Render as 3-button segmented control in a flex row with `gap-0` and shared border:
      - Active: `bg-brand-600 text-white border-brand-600`
      - Inactive: `bg-white text-neutral-700 border-neutral-300 hover:bg-neutral-50`
      - First button: `rounded-l-lg`, last: `rounded-r-lg`, middle: no rounding
    - Each button `onClick={() => onChange(action.value)}`

    4. Create `apps/web/src/components/scan/ScanInput.tsx`:
    - Props: `{ onScan: (qrCode: string) => void; disabled?: boolean; error?: string }`
    - Create `inputRef = useRef<HTMLInputElement>(null)`
    - `useEffect(() => { inputRef.current?.focus() }, [])` for auto-focus on mount (D-01)
    - Controlled input with local `buffer` state
    - `onKeyDown`: if `e.key === "Enter"`, `e.preventDefault()`, trim buffer, if non-empty call `onScan(trimmed)`, clear buffer, re-focus input
    - Large text input: `text-lg px-4 py-3 w-full` with rounded border
    - Placeholder: `"Scan QR code or type manually…"`
    - If `error` prop is truthy, render `<p className="mt-2 text-sm text-red-600">{error}</p>` below input (D-08)
    - `disabled` prop disables input and shows subtle opacity

    5. Create `apps/web/src/components/scan/CameraScanOverlay.tsx`:
    - Props: `{ open: boolean; onScan: (qrCode: string) => void; onClose: () => void }`
    - When `open` becomes true, initialize `Html5Qrcode` from `html5-qrcode` library in a `useEffect`:
      - Create instance with a container div ID (e.g., `"qr-reader"`)
      - Start scanning with `html5Qrcode.start({ facingMode: "environment" }, { fps: 10, qrbox: { width: 250, height: 250 } }, onScanSuccess, undefined)`
      - `onScanSuccess(decodedText)`: call `onScan(decodedText)`, then stop scanner, then call `onClose()`
    - When `open` becomes false or component unmounts, stop and clear the scanner
    - Render: fixed overlay (`fixed inset-0 z-50 bg-black/70 flex flex-col items-center justify-center`)
      - Header bar with "Scan QR Code" title and close (X) button
      - `<div id="qr-reader" />` container for the scanner viewfinder
    - Only render overlay content when `open === true`

    6. Create `apps/web/src/components/scan/ScannedPiecesList.tsx`:
    - Props: `{ items: Array<{ pieceId: string; shipmentId: string; newStatus: string; shipmentNumber: string; pieceNumber: number; scannedAt: Date }> }`
    - Render list of cards, most recent first (items are prepended by parent)
    - Each card: rounded border, padding, shows:
      - Shipment number (bold) + "Piece {pieceNumber}" label
      - New status as colored badge: `in_transit` → blue-100/blue-700, `delivered` → green-100/green-700, `completed` → purple-100/purple-700
      - Timestamp formatted as HH:MM:SS
    - Empty state: `<p className="text-sm text-neutral-400 text-center py-4">No pieces scanned yet</p>`
  </action>
  <acceptance_criteria>
    - grep: `apps/web/package.json` contains `html5-qrcode`
    - grep: `apps/web/package.json` contains `sonner`
    - grep: `apps/web/src/components/scan/scanSounds.ts` contains `playSuccessBeep`
    - grep: `apps/web/src/components/scan/scanSounds.ts` contains `playErrorBuzz`
    - grep: `apps/web/src/components/scan/scanSounds.ts` contains `AudioContext`
    - grep: `apps/web/src/components/scan/ActionSelector.tsx` contains `in_transit`
    - grep: `apps/web/src/components/scan/ActionSelector.tsx` contains `delivered`
    - grep: `apps/web/src/components/scan/ActionSelector.tsx` contains `completed`
    - grep: `apps/web/src/components/scan/ScanInput.tsx` contains `onScan`
    - grep: `apps/web/src/components/scan/ScanInput.tsx` contains `inputRef` and `.focus`
    - grep: `apps/web/src/components/scan/ScanInput.tsx` contains `Enter`
    - grep: `apps/web/src/components/scan/CameraScanOverlay.tsx` contains `Html5Qrcode`
    - grep: `apps/web/src/components/scan/CameraScanOverlay.tsx` contains `facingMode`
    - grep: `apps/web/src/components/scan/ScannedPiecesList.tsx` contains `shipmentNumber`
  </acceptance_criteria>
  <verify>
    <automated>ls apps/web/src/components/scan/scanSounds.ts apps/web/src/components/scan/ActionSelector.tsx apps/web/src/components/scan/ScanInput.tsx apps/web/src/components/scan/CameraScanOverlay.tsx apps/web/src/components/scan/ScannedPiecesList.tsx && grep -q "html5-qrcode" apps/web/package.json && grep -q "sonner" apps/web/package.json && echo "PASS"</automated>
  </verify>
  <done>html5-qrcode and sonner installed; all 5 scan utility components created with correct props, exports, and behaviors</done>
</task>

<task type="auto">
  <name>Task 2: ScanPage + route wiring + Toaster</name>
  <files>apps/web/src/pages/ScanPage.tsx, apps/web/src/App.tsx, apps/web/src/components/layout/AppLayout.tsx, apps/web/src/pages/ScanStubPage.tsx</files>
  <read_first>
    - apps/web/src/components/scan/ActionSelector.tsx (from Task 1)
    - apps/web/src/components/scan/ScanInput.tsx (from Task 1)
    - apps/web/src/components/scan/CameraScanOverlay.tsx (from Task 1)
    - apps/web/src/components/scan/ScannedPiecesList.tsx (from Task 1)
    - apps/web/src/components/scan/scanSounds.ts (from Task 1)
    - apps/web/src/App.tsx (route to update)
    - apps/web/src/components/layout/AppLayout.tsx (add Toaster)
    - apps/web/src/trpc.tsx (trpc import pattern)
  </read_first>
  <action>
    1. Create `apps/web/src/pages/ScanPage.tsx`:
    - Import: `useState` from react; `toast` from `sonner`; `trpc` from `../trpc`
    - Import all 5 scan components from `../components/scan/`
    - State:
      - `selectedAction`: string, default `"in_transit"`
      - `scannedItems`: array of `ScanResult & { scannedAt: Date }`, default `[]`
      - `error`: string or null, default `null`
      - `cameraOpen`: boolean, default `false`
    - `const processMutation = trpc.scan.process.useMutation({ onSuccess(data) { playSuccessBeep(); setScannedItems(prev => [{ ...data, scannedAt: new Date() }, ...prev]); toast.success(\`Piece \${data.pieceNumber} → \${data.newStatus.replace("_", " ")}\`); setError(null); }, onError(err) { playErrorBuzz(); setError(err.message); } })`
    - `function handleScan(qrCode: string)`: `setError(null)`, call `processMutation.mutate({ qrCode, action: selectedAction })`
    - Layout (top to bottom):
      - `<h2>` "Scan" heading
      - `<ActionSelector value={selectedAction} onChange={setSelectedAction} />`
      - `<ScanInput onScan={handleScan} disabled={processMutation.isPending} error={error} />`
      - Button: "Scan with Camera" (`onClick={() => setCameraOpen(true)}`), styled as secondary button with camera icon text, `mt-3`
      - `<CameraScanOverlay open={cameraOpen} onScan={handleScan} onClose={() => setCameraOpen(false)} />`
      - Divider or spacing
      - `<ScannedPiecesList items={scannedItems} />`

    2. Update `apps/web/src/App.tsx`:
    - Replace `import { ScanStubPage } from "./pages/ScanStubPage"` with `import { ScanPage } from "./pages/ScanPage"`
    - Change the `/scan` route from:
      ```tsx
      <Route path="scan" element={<RequireRole allowedRoles={["driver", "admin"]}><ScanStubPage /></RequireRole>} />
      ```
      to:
      ```tsx
      <Route path="scan" element={<ScanPage />} />
      ```
      This removes the RequireRole wrapper entirely — AuthGate already ensures authentication, and D-05 says any authenticated user can scan.
    - Remove the `ScanStubPage` import if unused elsewhere.

    3. Add Sonner `<Toaster />` to `apps/web/src/components/layout/AppLayout.tsx`:
    - `import { Toaster } from "sonner"`
    - Add `<Toaster position="top-right" richColors />` as a sibling just before the closing `</div>` of the root element

    4. Delete `apps/web/src/pages/ScanStubPage.tsx` (no longer needed)
  </action>
  <acceptance_criteria>
    - grep: `apps/web/src/pages/ScanPage.tsx` contains `trpc.scan.process.useMutation`
    - grep: `apps/web/src/pages/ScanPage.tsx` contains `ActionSelector`
    - grep: `apps/web/src/pages/ScanPage.tsx` contains `ScanInput`
    - grep: `apps/web/src/pages/ScanPage.tsx` contains `CameraScanOverlay`
    - grep: `apps/web/src/pages/ScanPage.tsx` contains `ScannedPiecesList`
    - grep: `apps/web/src/pages/ScanPage.tsx` contains `playSuccessBeep`
    - grep: `apps/web/src/pages/ScanPage.tsx` contains `playErrorBuzz`
    - grep: `apps/web/src/pages/ScanPage.tsx` contains `toast.success`
    - grep: `apps/web/src/App.tsx` contains `ScanPage`
    - grep: `apps/web/src/App.tsx` does NOT contain `ScanStubPage`
    - grep: `apps/web/src/App.tsx` contains `path="scan"` with `<ScanPage` (no RequireRole wrapper)
    - grep: `apps/web/src/App.tsx` does NOT contain `ScanStubPage`
    - grep: `apps/web/src/components/layout/AppLayout.tsx` contains `Toaster`
    - file: `apps/web/src/pages/ScanStubPage.tsx` does NOT exist
  </acceptance_criteria>
  <verify>
    <automated>grep -q "scan.process" apps/web/src/pages/ScanPage.tsx && grep -q "ScanPage" apps/web/src/App.tsx && ! grep -q "ScanStubPage" apps/web/src/App.tsx && grep -q "Toaster" apps/web/src/components/layout/AppLayout.tsx && echo "PASS"</automated>
  </verify>
  <done>ScanPage renders with action selector, RF input, camera button, error feedback, and accumulating scanned pieces list; /scan route accessible to any authenticated user; Sonner Toaster renders in layout; ScanStubPage deleted</done>
</task>

</tasks>

<verification>
- `pnpm --filter @material-tracking/web build` compiles without errors
- `/scan` route renders ScanPage without RequireRole wrapper
- ScanStubPage.tsx no longer exists
- Toaster component present in AppLayout
- All scan components import and compose correctly in ScanPage
</verification>

<success_criteria>
1. RF scanner input auto-focuses on page load and submits on Enter (SCAN-01, D-01)
2. Camera button opens html5-qrcode viewfinder overlay (SCAN-02, D-02)
3. Action selector defaults to "In Transit" with 3-way segmented control (D-03)
4. Success toast + scanned pieces list update on valid scan (D-07)
5. Inline red error below input on scan failure, clears on next attempt (D-08)
6. Audio beep on success, buzz on failure (D-09)
7. Any authenticated user can access /scan (D-05)
8. ScanStubPage deleted and replaced
</success_criteria>

<output>
After completion, create `.planning/phases/05-scan-processing-status-workflow/05-02-SUMMARY.md`
</output>
